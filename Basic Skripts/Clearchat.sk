command /clearchat:
	permission: nemus.skript.test
	permission message: hey, you are not allowed to use this.
	trigger:
		loop 250 times:
			send "" to all players
		send "%player's display name% &dhas cleared the chat!" to all players
